import { InputMaxValDirective } from './input-max-val.directive';

describe('InputMaxVal', () => {
  it('should create an instance', () => {
    //const directive = new InputMaxValDirective();
    //expect(directive).toBeTruthy();
  });
});
